vector<int> recordmax(vector<int>& nums)
{
	vector<int> ret;
	int n=nums.size();
	if(!n) return;
	stack<int> s;
	for(int i=0;i<n;i++)
	{
		if(!s.empty||nums[i]<=s.top())
		{
			fk this editer is so hard to use;
		}
	}
}
